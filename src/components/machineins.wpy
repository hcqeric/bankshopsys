<style lang="less">
  .order-item{
    position: relative;
    display: flex;
    padding: 60rpx 16rpx;
    background: #fff;
    justify-content: space-between;
    align-items: center;
    .order-item-left{
      display: flex;
      align-items: center;
      image{
        width: 56rpx;
        height: 50rpx;
        margin-right: 32rpx;
      }
      .order-info{
        display: flex;
        flex-direction: column;
      }
    }
    .order-item-right{
      display: flex;
      align-items: center;
      .text{
        margin-right: 8rpx;
      }
    }
    .order-type-button{
      position: absolute;
      top: 8rpx;
      padding: 8rpx;
      right: 32rpx;
      background: #fe5e48;
      font-size: 26rpx;
      color: #fff;
      border-radius: 0 0 16rpx 16rpx;
    }
    .order-type-button.done{
      background: #4a93db;

    }
  }
</style>
<template>
  <view class="order-item">
    <view class="order-item-left">
      <image src="../images/merchant.png"></image>
      <view class="order-info">
        <text>喜乐购超市</text>
        <text>13823154182</text>
      </view>
    </view>
    <view class="order-item-right">
      <text>2018-08-21</text>
      <image src="../images/arrow-right.png" class="arrow"></image>
    </view>
    <text wx:if="{{type == 0}}" class="order-type-button">待装机</text>
    <text wx:if="{{type == 1}}" class="order-type-button done">已装机</text>
  </view>
</template>
<script>
  import wepy from 'wepy';

  export default class MachineIns extends wepy.component {
      data = {

      }

    props = {
      merchantInfo: {
        type: Object
      },
      type: {
        type: Number,
        default: 0
      }

    }
  }
</script>
